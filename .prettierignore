# build output
dist/
.astro/

# dependencies and lockfiles (reformatting a lockfile invites merge conflicts)
node_modules/
pnpm-lock.yaml

# local-only scratch space
tmp/
brand/

# generated by scripts/generate-signature.mjs, and whitespace-sensitive: the
# wordmark is adjacent inline spans, and a reformat that puts a line break
# between two of them puts a space between two letters. Mail clients render that
# space, so the mark would go out as F A V I E N S.
docs/email-signature.html
